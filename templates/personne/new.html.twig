{% extends 'base.html.twig' %}

{% block title %}New Personne{% endblock %}

{% block body %}
    <h1>Create new Personne</h1>
    <div class="card p-2">
        {{ include('personne/_form.html.twig') }}
        <a class="btn btn-secondary btn-sm m-2" href="{{ path('app_personne_index') }}">Retour</a>
    </div>
{% endblock %}
