<form method="post" action="{{ path('app_objets_delete', {'id': objet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objet.id) }}">
    <button class="btn">Delete</button>
</form>
