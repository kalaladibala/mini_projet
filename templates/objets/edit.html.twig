{% extends 'base.html.twig' %}

{% block title %}Modifier un Objet{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="page-title">Modifier l'Objet</h1>

        <div class="form-container">
            {{ include('objets/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <div class ="pt-2 back-link">
            <a href="{{ path('app_objets_index') }}" class="btn btn-secondary">Retour</a>
        </div>
    </div>
{% endblock %}

